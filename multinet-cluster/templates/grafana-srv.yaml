apiVersion: v1
kind: Service
metadata:
  name: grafana-external-0
spec:
  selector:
    statefulset.kubernetes.io/pod-name: grafana-0
  ports:
    - name: frontend-port
      port: 80
      targetPort: 3000
      protocol: TCP
  type: NodePort
