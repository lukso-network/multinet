apiVersion: v1
kind: PersistentVolume
metadata:
  name: deposits-storage-test2
  labels:
    name: deposits-storage-test2
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  claimRef:
    name: deposits-storage-test2-pvc
    namespace: test
  mountOptions:
    - hard
  nfs:
    path: /
    server: nfs-server-deposit.default.svc.cluster.local