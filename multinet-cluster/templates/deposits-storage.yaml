apiVersion: v1
kind: PersistentVolume
metadata:
  name: deposits-storage-closed-testnet
  labels:
    name: deposits-storage-closed-testnet
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  claimRef:
    name: deposits-storage-pvc
    namespace: closed-testnet
  mountOptions:
    - hard
  nfs:
    path: /
    server: nfs-server-deposit.closed-testnet.svc.cluster.local
