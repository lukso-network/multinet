apiVersion: v1
kind: PersistentVolume
metadata:
  name: deposits-storage-dev3
  labels:
    name: deposits-storage-dev3
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  claimRef:
    name: deposits-storage-pvc-dev3
    namespace: dev
  mountOptions:
    - hard
  nfs:
    path: /
    server: nfs-server-deposit.default.svc.cluster.local
