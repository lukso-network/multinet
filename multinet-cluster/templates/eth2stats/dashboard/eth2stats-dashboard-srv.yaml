apiVersion: v1
kind: Service
metadata:
  name: eth2stats-dashboard-external
spec:
  selector:
    app: eth2stats-dashboard
  ports:
    - name: frontend-port
      protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort