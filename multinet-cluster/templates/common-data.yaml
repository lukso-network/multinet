apiVersion: v1
kind: PersistentVolume
metadata:
  name: common-data-test2
  labels:
    name: common-data-test2
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  claimRef:
    name: common-data-test2-pvc
    namespace: test
  mountOptions:
    - hard
  nfs:
    path: /
    server: nfs-server-data.default.svc.cluster.local